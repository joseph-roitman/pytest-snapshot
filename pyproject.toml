[build-system]
requires = [
  # sync with setup.py until we discard non-pep-517/518 (see pytest implementation)
  "setuptools>=40.0",
  "setuptools-scm",
  "wheel",
]
build-backend = "setuptools.build_meta"
